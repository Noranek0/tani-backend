[package]
name = "tani-backend"
version = "0.1.0"
edition = "2024"

[dependencies]
actix-web = "4"
clap = { version = "4.6.1", features = ["derive", "env"] }
diesel = { version = "2.2.0", features = ["postgres", "chrono"] }
dotenv = "0.15.0"
